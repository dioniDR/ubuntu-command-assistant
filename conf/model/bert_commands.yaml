model:
  name: "dccuchile/bert-base-spanish-wwm-uncased"
  type: "seq2seq"  # Usaremos un enfoque de secuencia a secuencia
  num_labels: 0    # No aplica para seq2seq
  max_length_input: 128
  max_length_output: 64
  hidden_dropout_prob: 0.1
